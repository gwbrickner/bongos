# Quick boot matrix for `make test`: one configuration per line, "<firmware> <cpus> [memMiB]"
# (memMiB optional, default 512 -- D-084; matrix-full.conf uses it for a heavier-memory row).
# Milestones enable more rows: BIOS rows in M2.5, 4-CPU rows in M3.5.
uefi 1
