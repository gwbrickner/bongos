# Decision log

This log is append-only. Each entry records a decision, the alternatives that were rejected,
and why. D-001 through D-046 were made by the owner during the planning session on
2026-09-24.

**Rules for new entries:**
- Changing an earlier decision needs a new entry that says "Supersedes D-xxx", plus an update
  to ARCHITECTURE.md in the same PR.
- Decisions that affect architecture need owner review: label the PR `needs-owner`.

| ID | Decision | Rejected | Why |
|---|---|---|---|
| D-001 | Goal: learning + daily driver + portfolio | single focus | Owner wants all three; drives "custom everything, but pragmatic ordering" |
| D-002 | Targets: QEMU first, then the owner's real PC | QEMU only; real HW first | Fast iteration + real-world payoff |
| D-003 | Kernel language: C17 + assembly (NASM) | Rust, C++, Zig | Owner choice |
| D-004 | Firmware: UEFI **and** legacy BIOS | UEFI only | Owner choice; both paths share a BootInfo ABI so the kernel doesn't care |
| D-005 | Own bootloaders from the start (no Limine/GRUB) | Limine | Owner choice: learn every piece |
| D-006 | UEFI loader first, BIOS loader a few milestones later | both first; BIOS first | Real PC uses UEFI; avoids weeks before first kernel output |
| D-007 | UEFI loader built on our own minimal headers written from the UEFI spec | gnu-efi, POSIX-UEFI | Fully custom; only ~12 protocols needed |
| D-008 | Boot media: one GPT disk image, USB-flashable, boots both ways (protective-MBR stage1 + BIOS boot partition + ESP) | hybrid ISO; both | Simplest path to real hardware |
| D-009 | Full SMP from day one; the kernel starts APs itself (INIT-SIPI-SIPI) | SMP later; single core | Owner choice; locking correct from the start |
| D-010 | Kernel architecture: hybrid (core subsystems in-kernel, IPC for optional userspace drivers) | monolithic; microkernel | Performance + crash isolation where it matters |
| D-011 | x86_64 only, with all CPU-specific code isolated in `arch/x86_64` | no abstraction; multi-arch now | Keeps an ARM64 port possible at near-zero cost |
| D-012 | In-kernel drivers loadable at runtime (`*.kmod`, exact ABI version match) | all built-in; userspace only | Flexibility; smaller kernel |
| D-013 | Scheduler: per-CPU run queues; classes realtime / normal (fair, vruntime) / background; idle stealing | round-robin; MLFQ | Desktop responsiveness |
| D-014 | Preemptible kernel | non-preemptible | Smooth input/audio; SMP locking already required |
| D-015 | VM features: demand paging + COW, memory-mapped files, swap, ASLR + KASLR | subsets | Owner wants all; ordered by dependency |
| D-016 | Compatibility: native API + POSIX layer in libc + Linux binary personality later | POSIX-native; no POSIX | Custom core, still ports software; Linux apps later |
| D-017 | Resources held as capability handles with rights; no ambient authority | Unix fds | Security; handles pass cleanly over IPC; POSIX fds map onto handles |
| D-018 | Native process creation is spawn-style; `fork()` emulated via the kernel `processClone` + COW (also used by Linux `clone`) | classic fork/exec; no fork | Clean native API; fork still available for ports |
| D-019 | Native FS: custom journaling "bongfs" (ordered mode, CRC32C); snapshots as a stretch goal | CoW FS; ext4 as native | Best reliability per effort |
| D-020 | Extra filesystems: FAT32 (required for ESP), ext4 (write gated), NTFS read-only, exFAT, ISO9660 | fewer | Owner choice |
| D-021 | One file tree + per-process namespaces | plain Unix tree; drive letters | Enables capability sandboxing |
| D-022 | Graphics: GOP/VBE framebuffer + virtio-gpu; no NVIDIA display driver; AMD iGPU display = stretch | real GPU modesetting/3D | Owner has an RTX 4060 Ti (infeasible); firmware framebuffer gives native resolution |
| D-023 | Drivers: USB (xHCI, hub, HID, storage), audio (HDA), Ethernet (e1000, virtio-net, RTL8125); no laptop support; Wi-Fi/Bluetooth on the later list | Wi-Fi now | Owner: "just Ethernet for now" |
| D-024 | Fully custom ACPI/AML interpreter (minimal early, full later) | port uACPI/ACPICA | Owner choice: fully custom |
| D-025 | Timers: invariant TSC + LAPIC one-shot/TSC-deadline, calibrated against the ACPI PM timer; HPET optional | require HPET | Some AM5 boards disable HPET |
| D-026 | Mandatory progress logging (STATUS.md + milestone logs, commit + push every working step) | — | Sessions can die on usage limits; the next session must resume cleanly |
| D-027 | Network stack in-kernel; NIC drivers as modules | userspace network server | Performance |
| D-028 | Network scope: IPv4 + IPv6, TCP, UDP, ICMP, DHCP, DNS, firewall, SSH server | smaller | Owner choice |
| D-029 | Custom crypto (TLS 1.3 client, SSH server, disk encryption), validated against official vectors + real-client interop, labeled EXPERIMENTAL | port a vetted library | Owner choice; honest labeling |
| D-030 | Shells: custom native `bsh` early, bash port later | one or the other | Shell needed before POSIX layer exists |
| D-031 | `svcd` service manager as PID 1, restarts crashed services and userspace drivers | simple init | Hybrid-kernel payoff |
| D-032 | Classic Unix directory layout | custom `/System`-style | Owner choice; ports expect it |
| D-033 | Custom package manager `pkg`, signed `.bpkg` packages, GitHub-hosted repo | none | Owner choice |
| D-034 | Self-hosting (port LLVM) as a late milestone | no | Owner choice |
| D-035 | Desktop: floating by default + tiling mode toggle | one mode | Owner choice |
| D-036 | Text: basic Unicode first; complex scripts + color emoji later | full Unicode first | Avoid an early rabbit hole |
| D-037 | Built-in apps: terminal, files, editor, settings, sysmon, imageview, player, calc, browser (no JavaScript) | fewer | Owner wants all |
| D-038 | SDL2 port + chocolate-doom with Freedoom | none | Owner choice |
| D-039 | Theme: dark, KDE Plasma-inspired layout with original assets | light; copied assets | Owner taste; keep assets legally clean |
| D-040 | Multi-user with login + `elevate` admin tool, Argon2id password hashes | single user | Owner choice; SSH needs it |
| D-041 | Sandbox GUI apps (namespace + `permd` portal); terminal tools unsandboxed | all or none | Owner choice |
| D-042 | Secure Boot with the owner's own keys; late milestone | none | Owner choice |
| D-043 | Full-disk encryption (AES-XTS + Argon2id), late, optional at install | none | Owner choice |
| D-044 | Public repo, MIT license | GPLv3; private | Portfolio; free unlimited CI minutes |
| D-045 | Merge policy: auto-merge on green CI + no Critical review findings, except `needs-owner` areas (memory, interrupts/SMP, scheduler, security/crypto, on-disk formats, boot ABI) | review all; merge all | Momentum with oversight of risky areas |
| D-046 | Name "bongOS"; per-phase cannabis-strain codenames; OS name only in `branding/`; identifiers use subsystem prefixes, camelCase functions, PascalCase types, UPPER_SNAKE constants | OS-name prefix in code | Owner choice; renaming stays a one-directory change |
| D-047 | Kernel built with `--emit-relocs`; loader applies the KASLR slide | PIE kernel | Keeps `-mcmodel=kernel` codegen; standard approach |
| D-048 | Initrd format: cpio newc | custom archive | Trivial to parse, host tools exist |
| D-049 | IPC protocols are hand-written C structs in `libs/proto`, no IDL compiler | IDL | Less tooling to build and maintain |
| D-050 | AMD-Vi IOMMU used to isolate userspace drivers; QEMU tests run with `amd-iommu` | trust drivers | Userspace drivers aren't isolated without it |
| D-051 | Every currently-empty ARCHITECTURE §2 directory gets a `README.md` naming what lands there and which milestone adds it | leave the directories out of git until populated | Keeps the repo tree matching ARCHITECTURE from day one; a reader doesn't need to open ARCHITECTURE.md to see the plan |
| D-052 | `branding.h` (generated by `tools/gen-branding.sh` from `branding/{name,version,codenames.tsv}`) exposes `BRANDING_NAME`/`BRANDING_VERSION`/`BRANDING_CODENAME`, not OS-prefixed; the codename is picked from the phase encoded in `branding/version`'s major.minor (mirroring the tag math in `.github/workflows/release.yml`), falling back to `"unreleased"` | prefixing the macros with the OS name | D-046 already rejects the OS name in code identifiers; renaming stays a one-directory change |
| D-053 | `make image`/`test`/`test-full` no-op cleanly (exit 0, clear message) while `boot/uefi/` and `boot/common/` have no `.c` sources; once they do, a missing build rule is a hard `$(error)` instead of a silent skip | make them fail now; or let them silently skip forever | ROADMAP M1.1 explicitly allows a no-op `test` until M1.2; the `$(error)` stops that no-op from surviving past the milestone that's supposed to replace it |
| D-054 | Host tests (`tests/host/`) use a constructor-registered `TEST()` macro, built with the host's own `clang` (not the cross toolchain) plus ASan/UBSan, one binary per `make host-tests` run | a TAP-style external runner; per-file binaries | Zero-config test registration; matches ARCHITECTURE §23 ("runs natively on Linux, fast"); sanitizers are cheap here and this is exactly the code (allocators, bongfs, crypto vectors, parsers) where they pay off most |
| D-055 | `.github/workflows/release.yml` skips compressing/publishing when `make RELEASE=1 image` produces no `build/bongos.img`, instead of failing the workflow | fail the release workflow on every milestone until M1.2 | Milestones before M1.2 have nothing to release; once boot sources exist, `mk/image.mk`'s own `$(error)` (D-053) makes a missing image a hard build failure, so this skip is self-limiting to the pre-M1.2 window |
| D-056 | GPT partition type GUIDs, minted with `cat /proc/sys/kernel/random/uuid` (the kernel's own RFC 4122 v4 generator), one call per constant, hardcoded in `tools/mkimage/gpt.c`/`gpt.h`: root = `D873F840-6583-4B38-8C8B-FA27F8332D8D` (`GPT_TYPE_GUID_ROOT`), swap = `1FC85B55-4413-4D12-8C5F-AD7BFAA6CB9D` (`GPT_TYPE_GUID_SWAP`, not yet used -- minted now so both format constants get owner review together). Each GUID names a partition **role**, not a filesystem: the root partition's actual contents are identified by their own on-disk magic (a bongfs superblock today; once full-disk encryption exists, ARCHITECTURE §15.2's crypt header wrapping one) -- a root partition with no recognized magic is unformatted, never corrupt, and must never be auto-formatted. The kernel finds root by scanning for `GPT_TYPE_GUID_ROOT` only on the disk whose GPT DiskGUID equals `BootInfo.bootDiskGuid`, never by scanning every disk | deriving the GUID from a name (UUIDv5); reusing an existing filesystem's type GUID (e.g. Linux's `0FC63DAF-8483-4772-8E79-3D69D8477DE4`, which would make Linux hosts probe and auto-mount it); an all-zero/reserved type until bongfs exists (UEFI §5.3.3 defines an all-zero type as "unused entry", so the space would look free and a tool could allocate over it); a GUID meaning "bongfs" specifically (breaks under FDE, where the partition holds a crypt header instead) | ARCHITECTURE §5.1 calls for "a custom type GUID recorded in DECISIONS.md" for root (and, optionally, swap); `tools/mkimage` (M1.2) already needs to label the empty partition 3 with its eventual role so real GPT tools (and later, the kernel's own GPT scanner, M6.4) can identify it, even though bongfs itself doesn't exist until M7.6. Reviewed by the `architect` subagent (docs/logs/M1.2.md) |
| D-057 | `tests/harness/run-qemu.sh` gained a `--expect-serial PATTERN` banner-match mode: PASS once PATTERN appears on serial (polling, killing QEMU on match/timeout), instead of the isa-debug-exit/KTEST exit-code protocol | wait for M1.3's kernel before wiring `make test` to the UEFI loader at all; give the M1.2 loader a fake KTEST report over serial | ARCHITECTURE §23's isa-debug-exit/KTEST protocol needs a kernel, which doesn't exist until M1.3, but ROADMAP M1.2's Done-when ("`make test` boots the loader under OVMF and sees the banner") needs `make test` to actually assert something now; extending run-qemu.sh (CLAUDE.md says extend, not rewrite) keeps one harness for both modes, and `mk/test.mk` drops `--expect-serial` the moment M1.3 lands |
| D-058 | `boot/uefi/include/efi/*.h` keep the UEFI spec's own naming inside type/struct/enum/macro bodies (UPPER_SNAKE typedefs like `EFI_GUID`/`EFI_STATUS`, PascalCase struct members and enum values like `AllocateAnyPages`/`EfiLoaderData`, `g`-prefixed globals like `gEfiRngProtocolGuid`) rather than ARCHITECTURE §4's camelCase/PascalCase/UPPER_SNAKE split | rewriting every spec name into our own convention | Spec traceability: these names are looked up directly against the UEFI spec text (and against real firmware's own headers) constantly while debugging a boot issue, and a renamed field is one more translation step in exactly the place where a transcription error is costliest to catch -- the `reviewer` subagent in fact caught one in this same milestone (`EFI_SIMPLE_FILE_SYSTEM_PROTOCOL_GUID`'s last byte written as `0x3e` instead of the spec's `0x3b`), which is the concrete case this decision is guarding against, not what motivated writing it down. The exception is scoped to `boot/uefi/include/efi/` only; `efiMain`/loader code everywhere else (`boot/uefi/main.c`, `serial.c`, `guids.c`'s own function-scope code) follows §4 as normal |
| D-059 | The UEFI loader HHDM-maps every RAM-backed EFI descriptor type it sees pre-ExitBootServices -- Loader{Code,Data}, BootServices{Code,Data}, Runtime{Code,Data}, Conventional, ACPIReclaim, ACPINVS -- clipped at the 64 TiB HHDM window, merging touching/overlapping runs first; MMIO, reserved, persistent, and unaccepted memory are never HHDM-mapped, and the kernel must not assume anything beyond USABLE/LOADER_RECLAIM/KERNEL/INITRD/ACPI_RECLAIM/ACPI_NVS (plus FRAMEBUFFER once M1.4 adds it) is reachable through it | mapping only USABLE memory; mapping everything below some fixed physical ceiling regardless of EFI type | A Runtime region must stay HHDM-reachable even if firmware frees it before ExitBootServices turns it USABLE later, and MMIO must never get a cacheable HHDM mapping (WB by default) or share a large page with real RAM -- SDM Vol 3A's "Large Page Size Considerations" calls that undefined over mixed MTRR types. Reviewed by the `architect` subagent (docs/logs/M1.3.md) |
| D-060 | `boot/common/memmap.c` maps each UEFI `EFI_MEMORY_TYPE` to a `BootMemType` (Reserved/Runtime -> RESERVED; Loader/BootServices/Conventional -> USABLE, with Conventional additionally downgraded to RESERVED when `EFI_MEMORY_SP` is set; Unusable -> BAD; ACPIReclaim/ACPINVS pass through; every other/OEM value -> RESERVED), then a single rank-sweep pass (`memMapNormalize`) rounds each input inward (USABLE) or outward (everything else) to page boundaries, sweeps the merged breakpoint set, and picks the highest-ranked input covering each interval -- doing sort, loader-overlay application, and coalescing in one pass | three separate passes (sort, then overlay, then coalesce) with ad hoc overlap rules; refusing to boot on any EFI type outside {Loader,BootServices,Conventional} | The rank sweep makes a loader overlay (KERNEL/LOADER_RECLAIM landing inside a larger Conventional/LoaderData descriptor) fall out for free instead of needing special-cased splitting logic, and it's what gives `bootMemMapCheck`/`bootInfoCheckRefs` and the `memmap_coalesced` ktest an array they can assert is sorted, non-overlapping, page-aligned, and coalesced by construction |
| D-061 | `kernel/arch/x86_64/kernel.ld` links the kernel higher-half at `0xFFFFFFFF80000000` as exactly four `PT_LOAD`s (text R-X, rodata+`.ktests` R--, data+bss RW-, and a NOBITS 16 KiB boot stack RW- sitting behind an unmapped guard page), built with `--orphan-handling=error` and `--emit-relocs` (kept for M2.6's KASLR relocation pass); the section-boundary symbols (`kernelTextStart`, `kernelBootStackTop`, ...) are camelCase, declared in `kernel/include/sections.h` | one RWX segment; a PIE kernel; boundary symbols named with a leading underscore or ALL_CAPS to mirror other kernels | Matches ARCHITECTURE §6.3's W^X-everywhere rule and §4's naming rules. `--orphan-handling=error` turns an accidental extra alloc section (e.g. a stray `.got`) into a link-time failure instead of it silently landing in whichever segment lld picks, possibly with the wrong permissions; the guard page turns a boot-stack overflow into a #PF instead of silently corrupting `.bss` |
| D-062 | The kernel entry stub (`entry.asm`) loads a temporary boot GDT (code+data descriptors with the Accessed bit preset, so the CPU never has to write one back against a read-only page once CR0.WP=1) via `lgdt` + far return, then a null IDT (limit 0) so any exception before the kernel installs its real IDT triple-faults immediately instead of running with a stale one. Kernel port I/O is split into a portable `kernel/include/arch/io.h` wrapper that includes the real x86 inline-asm implementation from `kernel/arch/x86_64/include/io-impl.h` -- a different filename, not `io.h` again, because a quoted `#include` searches the including file's own directory first and a same-named wrapper would only ever find itself | reusing the firmware's/loader's GDT and IDT (already unmapped under the kernel's own page tables at this point, ARCHITECTURE §5.4); naming the arch-backend header `io.h` too, matching the wrapper | Keeps the ARCHITECTURE §7.1 selector layout (0x08 code / 0x10 data) so M2.1's per-CPU GDT is a drop-in replacement rather than a renumbering; the same-name self-include was tried and verified to silently expand to nothing during this milestone, which is why the backend header has its own name |
| D-063 | The `.ktests` linker section holds one `const KtestCase *` pointer per `KTEST()` (not the struct inline), so every entry in the array is a uniform, 8-byte, 8-byte-aligned pointer -- entries contributed by different translation units can never end up with padding between them, keeping the `ktestsStart`/`ktestsEnd` array walk safe (storing a pointer actually costs one *more* relocation than a repointed struct would, once KASLR exists, since the pointed-to struct's own internal pointers still need relocating too); the wire protocol is exactly ARCHITECTURE §23's raw, unprefixed `KTEST START`/`PASS`/`FAIL`/`DONE` lines, and `archDebugExit`'s port-0xF4 write only happens when `ktest=` is on the command line. The harness now builds a second image, `build/bongos-ktest.img` (`tests/harness/ktest-boot.cfg` bakes in `cmdline=ktest=all`), and `mk/test.mk` points `make test`/`test-full` at it instead of `build/bongos.img`, dropping the M1.2-era `--expect-serial` flags (D-057) now that the real isa-debug-exit/KTEST protocol exists | embedding `KtestCase` structs directly in `.ktests`; adding a `ktest=` cmdline override to the one release image instead of building a second one; keeping `--expect-serial` as the pass/fail signal | D-057 already said M1.3 must drop `--expect-serial`. A second image keeps the release/USB image from ever writing port 0xF4 or running ktests -- 0xF4 isn't a defined port on real hardware, and ARCHITECTURE's safety posture is that test-only behavior never ships |
| D-064 | BootInfo v1: a zero value means "not provided" for `fb.phys`, `initrdPhys`/`initrdSize`, `rsdpPhys`, `efiSystemTablePhys`, and both GUIDs; a `version` mismatch is refused outright, with no forward/backward compatibility attempted in v1. Validation is split into three pure functions -- `bootInfoCheckHeader`, `bootMemMapCheck`, `bootInfoCheckRefs` -- that take their data as explicit arguments rather than reaching for globals | one monolithic `bootInfoValidate` with no internal seams; treating a version mismatch as a warning and trying to proceed anyway | Matches ARCHITECTURE §5.3's "if the kernel sees a different version, it refuses to boot" language. The three-way split is what makes `bootinfo_rejects_bad`'s seven corruption cases (bad magic, wrong version, wrong size, unsorted map, overlapping map, `type=0`, non-NUL-terminated cmdline) testable by feeding hand-built corrupted copies straight to the relevant helper, instead of needing a separate QEMU boot per case |
| D-065 | `boot/common/**` (shared by the UEFI loader today, the BIOS loader in M2.5, and host tests) never uses `long` (32-bit under the Windows x64 ABI) or 64-bit `/`/`%` (would pull in `__udivdi3` once the i386 BIOS target exists); every physical address becomes a pointer only through `bootPhysToPtr()`; and its own `bootMemcpy`/`bootMemset` are named apart from libc's `memcpy`/`memset`, which `kernel/core/string.c` and `boot/uefi/libc-shim.c` each shim separately for their own freestanding target's implicit compiler calls | sharing one `memcpy`/`memset` implementation, under the plain libc names, across all three build targets; letting `boot/common` use `long`/division freely and fixing fallout when the i386 BIOS target lands | `boot/common/*.c` already compiles for two real targets (x86_64-windows, and the host's native clang) and a third, i386-elf, is coming in M2.5 -- catching a `long`/division mistake now, while it's still cheap to fix, beats debugging it once the BIOS loader exists and the mistake is buried in loader code nobody's touched in months |
| D-066 | The final CR3 swap lives in its own 64-byte, 64-byte-aligned NASM routine (`boot/uefi/trampoline.asm`, assembled with `nasm -f win64`) whose containing page gets an explicit R-X identity mapping in the new page tables (deliberately not marked global, since it's discarded the moment the jump completes); it touches no memory between `mov cr3` and `mov rsp`. The loader's LOADER_RECLAIM handoff block is one `AllocatePages` call split into fixed pages -- page 0 is BootInfo, page 1 is the cmdline, the rest hold the `BootMemRegion` array -- rather than three separate allocations | writing the trampoline in C; giving BootInfo, the cmdline, and the memory-map array their own separate `AllocatePages` calls | SDM Vol 3A §4.10.4.1 requires toggling CR4.PGE around the CR3 load to flush the firmware's global TLB entries, which has no C equivalent, and a C compiler is otherwise free to spill to the stack or touch memory in the exact window where nothing is mapped. One allocation for the whole handoff block means one LOADER_RECLAIM overlay entry instead of three, which keeps today's memory map smaller and M2.2's eventual reclaim simpler |
| D-067 | boot.cfg grammar: line-oriented, UTF-8 BOM skipped, NUL rejected, 64 KiB max; `#` comments at line start only (no inline comments); `[Name]` headers whose trimmed inner text (1-63 bytes, 0x20-0x7E, no brackets, unique) is the menu display name (no `name=` key); at most 9 entries (digit keys 1-9); keys `[a-z0-9_.]{1,31}`, unknown keys ignored but counted, a non-blank, non-comment line without `=` is an error; `kernel`/`initrd`/`cmdline`/`resolution`/`kaslr` valid globally (before the first section) and per entry, with entry, then global, then built-in (`/bong/kernel.elf`, none, "", auto, on) inheritance, and an explicit empty `initrd`/`cmdline` overriding the inherited value; `timeout` (0-3600 or `forever`, default 0 = no menu) and `default` (1-based index if all digits, else an exact name; default 1) are global-only; first occurrence wins within a scope; zero sections gives one implicit entry, so M1.3 files boot unchanged; every error is fatal with a line number, and `tools/mkimage` runs the same parser at image-build time. The parser is two-phase: zero-copy spans into the file buffer (`BootCfg`, about 600 B), then `bootCfgResolveEntry` copies the chosen entry into `BootCfgEntry` | a separate `name=` key; `+=` cmdline append; last-wins duplicates; per-entry `timeout`; copying every entry's 4 KiB cmdline at parse time; ignoring malformed lines | Name-in-brackets is the least syntax for a menu file; 9 entries make serial selection a single keystroke; spans keep the struct small enough for M2.5's sub-1 MiB stage2 and stay D-065-clean; build-time validation plus strict runtime errors catch typos before they reach a USB stick. Designed with the `architect` subagent (docs/logs/M1.4.md) |
| D-068 | The loader picks the GOP from `ConsoleOutHandle`, falling back to the first `LocateHandleBuffer` GOP with a linear framebuffer. Mode rule: accept pixel formats 0/1/2 with 32-bit pixels only; auto = largest W×H with W≤3840 and H≤2160 (ties to larger W, then lower mode number); `WxH` = exact match, else auto. The mode is set before the menu using the global `resolution`, and set again after selection if the entry's resolution differs. After the first `SetMode` the loader never uses `ConOut`; menu input comes from `ConIn` only, and COM1 RX is never polled before ExitBootServices. `BootFramebuffer.pitch` is in bytes. The framebuffer is the one non-RAM HHDM mapping: 4 KiB pages, PCD=1/PWT=0/PAT=0 (UC- under the power-on PAT), NX, global, plus a `BOOT_MEM_FRAMEBUFFER` overlay; if it overlaps an existing mapping or lies beyond the 64 TiB window, `fb` is zeroed. Supersedes D-059's "MMIO is never HHDM-mapped" for this one range only | a fixed virtual address in the kernel virtual area (a new handoff constant); a new BootInfo field (version bump); a WB mapping; large pages; reading keys from COM1 directly | D-059 already promised FRAMEBUFFER would be HHDM-reachable from M1.4. 4K UC- is never cacheable, never shares a large page with RAM, and becomes WC wherever firmware's MTRRs say so. BootInfo stays v1. OVMF's TerminalDxe owns COM1 RX and GraphicsConsole draws at stale geometry after a direct `SetMode`. M2.3 replaces the mapping with WC. Designed with the `architect` subagent (docs/logs/M1.4.md) |
| D-069 | The console font is original work: `data/fonts/console.txt` ('#'/'.' 8×16 grids, the source of truth) compiled by `tools/mkfont` into a checked-in PSF2 `data/fonts/console.psf` (128 glyphs, flags 0, 2080 bytes), with `make font-check` (run from `format-check`) keeping the two in sync; linked into both the loader and the kernel as a generated C array (`fontConsolePsf`), not loaded from disk. fbcon: palette of 16 colors in ANSI order, integer scale `clamp(min(W/1280, H/720), 1, 4)`, no cursor, a static cell shadow, and never reading the framebuffer back | an OFL PSF (Terminus etc.); the VGA ROM font; loading the font from the ESP via a new BootInfo field | Avoids OFL license-accompaniment obligations inside `BOOTX64.EFI` and `kernel.elf`, and any question about where the glyphs came from; matches ROADMAP's "original or public domain"; embedding needs no ABI change and works even when ESP reads fail. Designed with the `architect` subagent (docs/logs/M1.4.md) |
| D-070 | GUI tests capture PPM through QMP `screendump` at serial sync points, never on a timer: the loader's `loader: menu ready` (with `timeout = forever` in `tests/gui/menutest-boot.cfg`, at a pinned 1024×768 on `-vga std`), and the `fbcon_screenshot` ktest's `SCREENSHOT kernel` + wait for a `\n` ack (only when `ktest.shots=1` is on the cmdline; RX FIFO drained first). `tools/imgdiff` has its own PPM reader and PNG codec (full inflate; fixed-Huffman deflate); comparison is exact by default, with optional per-channel/pixel-count tolerance and `x y w h` mask sidecars. References live in `tests/gui/ref/[<fw>-]<shot>.png`, regenerated only by `make update-refs` with a log entry. `run-qemu.sh --script` drives runs via `tests/harness/qemu-script.py` (`expect`/`send`/`screendump`, QEMU started with `-S`, sockets connected, then `cont`) | fixed sleeps before screendump; QEMU's `-f png` (depends on the QEMU build's libpng); fuzzy matching by default; capturing the real post-banner boot screen | All rendering is ours and deterministic, so exact matching catches one-pixel glyph bugs; sync markers remove timing flakiness; the real boot screen changes with OVMF's memory map and with every version bump. Designed with the `architect` subagent (docs/logs/M1.4.md) |
| D-071 | Five local M1.4 implementation choices the `reviewer` subagent's post-milestone pass surfaced as undocumented: (1) D-068's HHDM framebuffer mapping checks every 4 KiB page in `[fbBase, fbEnd)` for a conflicting existing mapping (not just the range's first/last page), and reads `IA32_PAT` (MSR 0x277) back to confirm entry 2 is actually UC/UC- before trusting the mapping's cacheability, refusing the framebuffer (falls back to "not provided", D-064) rather than assuming the documented power-on default holds; (2) `fbcon`'s `scroll()` redraws only the shadow cells that actually changed value, not the whole screen unconditionally, since a full redraw against a UC-/UC-mapped framebuffer (until M2.3 remaps it WC) is measurably slow; (3) the boot menu runs whenever `timeout > 0` even with no usable GOP framebuffer -- serial-only (`BootFbText *fx = NULL`, which every `fbText*` call already tolerates) instead of being skipped outright, matching ARCHITECTURE §5.2's "menu on screen and serial" wording; (4) errors after the first GOP `SetMode` go to raw COM1 only, not also to the framebuffer text renderer -- ARCHITECTURE §5.5 corrected to match, rather than building out fbtext error rendering for the loader's error paths, since every one of them already has a serial diagnostic and a boot that fails before the menu has no menu screen to show one on anyway; (5) `tools/imgdiff`'s from-scratch DEFLATE encoder (`deflateFixed`) only ever tries two candidate back-reference distances per position -- 3 (PNG's own RGBA-filtered-byte stride) and the image's row stride -- instead of a full sliding-window LZ77 search, since PNG-filtered screenshot data is overwhelmingly redundant along exactly those two axes (a filtered pixel repeating the one before it, a filtered row repeating the one above it) and a real search buys negligible extra compression for a test-only encoder that never has to beat general-purpose deflate | a full LZ77 window search in imgdiff; leaving the boot menu framebuffer-only; a full fbtext-based error UI; spot-checking only the framebuffer range's endpoints; trusting the documented PAT power-on default without reading it back | None of these were wrong, but none were written down either, and a reviewer with no memory of the implementation session had to re-derive the reasoning from the diff alone each time -- recorded here per CLAUDE.md's "small, local choice: decide, then add a D-0xx entry" rule, in one entry since they're all narrow, low-risk implementation details from the same review pass rather than separate design decisions |
| D-072 | M2.1 GDT/TSS: the full §7.1 selector layout is installed now, BSP-only, as one static `ArchCpuTables` built at runtime (a TSS base address can't be expressed in a static initializer, and runtime construction is KASLR-safe): null (0x00), kernel CS (0x08), kernel DS (0x10), user CS32 placeholder (0x18, **not present** -- SYSRET never reads it, and a present compat-mode descriptor is needless attack surface before there's any userspace), user DS (0x20), user CS64 (0x28), TSS (0x30, 16 bytes). All descriptors have the Accessed bit preset (D-062's reasoning). TSS: RSP0=0 (poison -- there are no ring transitions yet, so any stray one #PFs near-null instead of silently using a stale stack), IST1/2/3 map to #DF/NMI/#MC, no I/O bitmap (`iomapBase` = TSS limit+1). `gdtBuild`/`tssBuild` take an explicit struct pointer so M3.5 can call them per-CPU and move the instance into `CpuLocal` without a rewrite | installing user descriptors only once userspace exists (selector churn later); a present code32 descriptor; per-CPU tables now (SMP bring-up is M3.5) | Selector stability matters once STAR is programmed (`GDT_USER_CS64_RPL3`/`GDT_USER_DS_RPL3` reserved now); RSP0=0 turns a stray privilege transition into a loud, diagnosable fault instead of silent corruption. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-073 | Supersedes D-061's "exactly four `PT_LOAD`s": `kernel.ld` gains three more, one per IST stack (16 KiB NOBITS each, IST1-3 for #DF/NMI/#MC), each preceded by an unmapped guard page -- a guard page is a gap between segments, so each stack needs its own `PT_LOAD` rather than sharing one. Seven `PT_LOAD`s total (`ELF_MAX_SEGMENTS`=8 already accommodates it); no further static stack may be added without also raising that cap | stacks in `.bss` with no guard page; a shared PT_LOAD for all three stacks (no gap between them, so an overflow from one silently corrupts the next) | An unguarded IST stack overflow during exactly the kind of severe fault (#DF/NMI/#MC) it exists to report would corrupt kernel memory instead of #PF-ing loudly. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-074 | All 256 IDT entries are interrupt gates (`attr=0x8E`, DPL0; vector 3 is `0xEE`, DPL3, for a future user `int3`) with `%rep`-generated stubs in `kernel/arch/x86_64/trap-entry.asm` and a 176-byte `TrapFrame`. Every entry is populated (not just 0-31) so a stray vector takes a diagnosable #GP/#DF chain instead of triple-faulting off a limit-0 IDT. `trapDispatch`'s first statement reads CR2, before anything else can clobber it. Every unhandled vector panics in M2.1 except #BP (resumes via a plain `iretq` -- #BP is a trap, not a fault, so RIP already points past the `int3` byte and must not be adjusted); NMI/#DF/#MC always panic (no legitimate source exists yet, and CR4.MCE isn't set until M3.6). IF stays 0 for the whole milestone -- no IRQ source is wired up before M3.2 | trap gates (would leave IF set during the handler); installing only vectors 0-31; auto-advancing RIP past `int3`; treating NMI as resumable | A not-present gate on the wrong vector is worse than a wrong-but-present one: it's an unreadable double/triple fault instead of a printed report. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-075 | KSYM v1: a from-scratch, versioned, little-endian kernel-symbol-table format (spec in `docs/specs/ksyms.md`) -- header + block index (binary-searchable) + a byte-pair-token dictionary (tokens 0x80-0xFF over 7-bit ASCII names) + a per-symbol delta-address/encoded-name stream, built only from `STT_FUNC` symbols in executable sections. `tools/ksyms` (a host tool, same precedent as `tools/mkfont`/`tools/imgdiff`) does a two-pass link: link once with an empty placeholder blob, extract real symbols from that first `kernel.elf`, generate the real blob, relink, then `ksyms check` re-extracts and byte-compares against what shipped, failing the build (and deleting the bad ELF) on any mismatch. `symbolize()` (ROADMAP's name) ships as `ksymSymbolize()`, backed by a pure, host-testable `ksymDecodeLookup()` that bounds-checks every read and returns NOT_FOUND on any malformed input rather than faulting | a deflate-compressed table (needs an inflate implementation and buffers reachable during a panic, when the allocator may itself be the thing that's broken); no compression (ARCHITECTURE §24 calls for "compressed"); a separate blob the loader loads (a BootInfo ABI/version bump for something that's pure kernel-image content) | Two-pass-link-plus-verify is the only way to embed a table of the *final* binary's own symbol addresses inside that same binary without a chicken-and-egg problem, and matches the project's established from-scratch-host-tool precedent instead of inventing a new embedding mechanism. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-076 | The UBSan runtime (debug builds only, `-DKERNEL_UBSAN=1`) uses an explicit check list -- `alignment,bool,builtin,bounds,enum,integer-divide-by-zero,nonnull-attribute,null,object-size,pointer-overflow,returns-nonnull-attribute,shift,signed-integer-overflow,unreachable,vla-bound` -- never the bare `-fsanitize=undefined` group, and never `local-bounds` (emits a bare `ud2`, no handler call). `__ubsan_handle_*` data structures (`SourceLocation`, `TypeDescriptor`) are written from the compiler ABI, not copied from any runtime. Every check always panics (through `archTrapCatchSoftware` first, so a ktest can still catch a deliberate one) -- UB is always a bug in a debug/CI-only build, never something to log-and-continue past | the bare `-fsanitize=undefined` group (clang 18 folds in `function` for C, which prefixes every function with an 8-byte type hash checked on indirect calls -- confirmed by the architect's probe that this reads unmapped memory before `kernelEntry`, since hand-written asm entry points carry no such prefix); a minimal handler with no source location; non-fatal/log-only UBSan trips | Confirmed by direct compiler probes, not just spec-reading, that the bare flag group is actively unsafe for this codebase's mixed C/asm entry points. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-077 | Supersedes ARCHITECTURE §6.6's "seeded from [the CSPRNG] at boot" wording: `__stack_chk_guard` is set exactly once, from a splitmix64-style fold of all 8 qwords of `BootInfo.randomSeed` mixed with one `rdtsc` reading, with its lowest byte forced to 0 (so the canary also acts as a string terminator against a buffer over-read) and never left exactly 0. The reseed function is both `noinline` **and** `no_stack_protector` (confirmed by compiler probe: `no_stack_protector` alone doesn't stop clang 18 from inlining the call into a protected caller, which would then fail its own canary check on return) and is called from `kernelMain`, itself marked `no_stack_protector`, before any other protected frame that outlives it. The guard is never reseeded again -- M2.6's CSPRNG consumes and wipes the live seed instead | reseeding periodically or per-thread later without a documented "last legal reseed point"; relying on `no_stack_protector` alone without also forcing `noinline` | A canary that can silently stop protecting frames because the reseed function got inlined is worse than no reseed at all -- confirmed as a real risk by direct compiler probe, not assumed from the attribute's name. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-078 | `archTrapCatch(mask, fn, arg, &info)`: a setjmp/longjmp-style, ktest-only, one-shot, non-nesting fault-catching mechanism (`kernel/arch/x86_64/trap-entry.asm`'s `archTrapCatchCall`/`archTrapCatchResume`, callee-saved-register save/restore, no signal-safety concerns since this is single-threaded kernel code with IF=0). `trapDispatch` checks an armed catch's vector mask before falling through to `panic()`; on a matching *hardware* fault it disarms, prints the full report (still exercising the symbolized backtrace), and redirects the `iretq` to a resume stub instead of panicking. Two additional catch kinds beyond CPU vectors, `TRAP_CATCH_STACK_SMASH` and `TRAP_CATCH_UBSAN`, are raised via `archTrapCatchSoftware()` from `__stack_chk_fail` and the UBSan handlers when a catch is armed -- a matching *software* trip is deliberately silent on serial (no report printed) and redirects immediately, since the point of catching it is to avoid the loud panic a real, uncaught trip still produces; the ktest itself reports PASS/FAIL. Can never catch NMI/#DF/#MC (D-074 keeps those always-fatal) | a global "expected fault" flag checked before an unconditional panic, resuming at a fixed hardcoded RIP (the C compiler's live register/stack state at the fault point isn't otherwise recoverable); making the mechanism generally available outside ktest (`archTrapCatch` panics immediately if `!ktestIsActive()`) | The only way to test "does the kernel correctly detect and report X fault" without either skipping the test (forbidden by CLAUDE.md) or panicking the whole kernel (which would end the ktest run) is to actually let the fault happen for real and recover control afterward. Designed with the `architect` subagent (docs/logs/M2.1.md) |
| D-079 | The `Page` metadata array (ARCHITECTURE §6.2): a 64-byte `Page` (state/order/flags/refcount/mapcount/an intrusive `ListNode` free-list link/`object`+`objectIndex` stubs for M4's VmObject/a private word M2.4's slab allocator uses) at the fixed VA region `0xFFFFE00000000000`-`0xFFFFEFFFFFFFFFFF` (ARCHITECTURE §6.1), indexed by pfn with plain integer arithmetic (`pageFromPfn`/`pageToPfn` -- never pointer subtraction, so a huge or bad pfn can't trip UBSan's pointer-overflow check). Backing is sparse: only pfn ranges the BootInfo map actually reports as USABLE/LOADER_RECLAIM/KERNEL/INITRD/ACPI_RECLAIM (clipped to the 64 TiB HHDM window) get Page entries, widened to 1024-frame (order-10) envelopes and merged -- a free block is always naturally aligned and at most order 10, so its buddy is always inside the same envelope and a merge never needs a separate validity check. A zero-filled entry is `PAGE_STATE_RESERVED` by construction, so mapping in a freshly bump-allocated (already-zeroed) page-array page needs no separate init pass | a dense array up to the highest reported physical address (would cost gigabytes of metadata for a single high RESERVED region on real hardware); a separate free-block bitmap instead of state living in the Page array itself; embedding the array in `.bss` or the HHDM | Sparse-but-envelope-widened backing gets the "no validity check on merge" property of a dense array without its worst-case memory cost. Designed with the `architect` subagent (docs/logs/M2.2.md) |
| D-080 | The early bump allocator (`kernel/mm/early.c`), used only inside `pmmInit()` to back the Page array and its own page tables before the buddy allocator exists: walks the BootInfo USABLE ranges top-down (highest range first, so NORMAL-zone memory is consumed before the scarcer DMA32 range), zeroing every allocation via its own qword loop (not a call to `kernel/core/string.c`'s `memset`, which stays a plain byte-at-a-time loop -- fine for its own small, infrequent uses, but too slow under TCG for the megabytes the Page array's own bump-zeroing touches) so mapping in Page-array pages needs no extra init pass. USABLE memory below 1 MiB is withheld from the buddy allocator entirely (reserved for the M3.5 SMP trampoline and as a hedge against BIOS-area memory some firmware reports USABLE but isn't safe to write). Before any bump zeroing happens, `pmmInit()` walks every managed BootInfo *region* (not the widened Page-array spans, whose alignment padding can cover physical holes -- like the legacy 0xA0000-0x100000 VGA/BIOS gap -- that never appeared in the map at all and so carry no HHDM guarantee) through `archEarlyLookup()` and panics if any of it isn't already identity-mapped through the HHDM exactly as D-059 promises, since the loader's own memory-map code admits it never checks this itself | a single contiguous early-allocation pool; bottom-up consumption (would eat into the scarcer DMA32 range first); trusting the loader's HHDM promise without checking it; walking the widened spans for the HHDM check (caught a real boot hang in this milestone's own QEMU testing -- see docs/logs/M2.2.md) | Bootstrapping the allocator that will eventually own all of memory, using only itself, is the classic chicken-and-egg problem; consuming from the top and reserving low memory sidesteps two different real hazards (DMA32 scarcity, BIOS-area corruption) for free. Designed with the `architect` subagent (docs/logs/M2.2.md) |
| D-081 | The buddy allocator (`kernel/mm/buddy.c`, pure/host-tested) and its public API (`kernel/include/pmm.h`): orders 0-10 (4 KiB-4 MiB) in two zones fixed by address -- DMA32 below 4 GiB, NORMAL at or above it, chosen by `pmmZoneOfPfn()` rather than stored per-page, with an order-10-aligned boundary so a block can never straddle it. Block free/allocated state lives entirely in the Page array (`PageState`: RESERVED/TAIL/BUDDY/PCP/ALLOCATED) -- no separate bitmap. `pmmAllocPages(order, flags, Page**)`/`pmmFreePages(page, order)` (Status + out-param, never a sentinel; free can't fail -- any misuse is a kernel bug, D-082) is the only public surface; `pmmAddFreeRange(physBase, length)` is the *only* way memory ever enters a zone (used by `pmmInit()` for BootInfo's USABLE ranges, and reused as-is by M2.3 for LOADER_RECLAIM, M3.1 for ACPI_RECLAIM, and M5.5 for INITRD -- no second bump-then-buddy pass is ever needed, since a reclaimed range's Page entries already exist in state RESERVED from D-079's sparse backing). A BSP-only per-CPU cache (`PMM_PCP_BATCH`=32, `PMM_PCP_HIGH`=192, one free list per zone so DMA32-flagged order-0 allocations get cached too) fronts every order-0 request through a single internal `pmmLocalCache()` accessor -- no cpu index appears in any public call -- so M3.5's real `CpuLocal` can replace that one accessor's body with nothing else in this file changing | a bitmap alongside the Page array; a zone chosen per-page and stored redundantly instead of computed from address; letting M2.3/M3.1/M5.5 reinitialize the bump-then-buddy pipeline a second time for their reclaimed ranges; a real per-CPU cache array indexed by cpu id before SMP exists | `pmmAddFreeRange` as the single memory-entry point is what makes every later milestone's reclaim a one-line reuse instead of a parallel code path; computed zone membership can never drift out of sync with a page's actual address the way a stored-but-wrong zone field could. Designed with the `architect` subagent (docs/logs/M2.2.md) |
| D-082 | pmm misuse detection (`kernel/mm/pmm.c`'s `pmmValidateForFree`): a state-machine check, always on (not just debug builds), run *before* any lock is taken or any state mutated, since a ktest's `archTrapCatch` resume is a longjmp that would skip `pmmUnlock()` entirely and leave interrupts disabled forever if a bug were ever reported mid-mutation. A `TAIL`-state page (not a block head) is distinguished from a genuine double-free by probing, for every alignment `k` from 0 to `PMM_MAX_ORDER`, whether the `k`-aligned floor of its pfn is currently a free `BUDDY` head of order >= `k` -- provably covers the page in question if so (both are multiples of 2^k), meaning it was already returned to the allocator. A detected bug calls the new `panicBug()` (`kernel/include/panic.h`), which extends D-078's `archTrapCatch` with a third software-trip kind, `TRAP_CATCH_KERNEL_BUG`, exactly mirroring `__stack_chk_fail`'s/UBSan's existing catch-then-panic pattern. `KERNEL_DEBUG` (new: `-DKERNEL_DEBUG=1` in debug builds, alongside the existing `KERNEL_UBSAN`) additionally poisons a freed block's full physical content with a fixed non-canonical qword pattern and verifies it on the next allocation, catching a write-after-free the state machine alone can't see | debug-only misuse detection (ROADMAP only requires it in debug builds, but the state machine is cheap enough -- O(1) plus at most 11 probes -- to run unconditionally, and a double free is exactly the kind of bug that should never silently succeed in a release kernel either); validating after taking the lock (would deadlock/hang IRQs forever on the very first caught bug) | The TAIL-probe proof is what makes double-free detection correct even for a page that has already merged into a larger free block, not just the simple case of an unmerged head. Designed with the `architect` subagent (docs/logs/M2.2.md) |
| D-083 | Supersedes ROADMAP M2.2 step 4's "reclaim LOADER_RECLAIM memory after switching stacks": that reclaim moves to M2.3, after the kernel switches to its own page tables and toggles CR4.PGE (which M2.3 needs anyway, since the loader's HHDM/kernel mappings are marked Global) -- reclaiming it in M2.2 would free the *live* page tables (and the loader's identity-mapped trampoline page) the CPU is still using for address translation. Flagged as a STATUS.md "Questions for owner" item during M1.3's design and left unresolved until this milestone's `architect` consultation confirmed the fix. M2.3 hands the reclaimed range to `pmmAddFreeRange` (D-081) using the pmm's own copy of the LOADER_RECLAIM ranges (`PmmMap.loaderReclaim[]`, recorded but not freed in M2.2) -- no second bump-then-buddy pass, and no BootInfo/loader-map re-read (M2.3 must stop using `kernelBootInfo()`'s live pointer once this reclaim runs) | reclaiming in M2.2 as ROADMAP originally worded it (would corrupt live address translation); a parallel reclaim code path instead of reusing `pmmAddFreeRange` | ROADMAP.md's M2.2 section and STATUS.md's "Questions for owner" item are updated in this same PR now that the fix is confirmed rather than just proposed. `needs-owner: yes` for this one, since it changes a ROADMAP milestone's steps. Designed with the `architect` subagent (docs/logs/M2.2.md) |
| D-084 | `tests/harness/matrix-full.conf` gained an optional third column, `<fw> <cpus> [memMiB]` (`run-matrix.sh`/`mk/test.mk`'s `_check-ktest-pass` both grow a `-<mem>m` log-name suffix when it's present): a `uefi 1 3072` row, since q35 splits that into ~2 GiB below the 4 GiB PCI hole and the rest above it, is the only configuration in the whole harness that actually exercises the NORMAL zone's code paths (the default 512 MiB `make test` row never populates it at all). Left out of `matrix.conf` itself (kept fast, matching its own "quick boot matrix" comment) -- `make test-full` is the "heavier scenarios" row matrix-full.conf's own header already calls for | adding the row to matrix.conf too (slows down every `make test`); a fixed-size dedicated memory-only test image instead of a matrix row (would duplicate the whole harness for one extra QEMU flag) | The pmm's zone-selection/fallback logic (DMA32 vs. NORMAL) is real code that a 512 MiB CI configuration structurally cannot exercise -- worth the one extra `make test-full` row rather than leaving it untested until some later milestone accidentally boots with more RAM |
| D-085 | Two known limitations the `reviewer` subagent's M2.2 pass surfaced, deliberately left as-is for this milestone (BSP-only, IF=0, no real concurrency) rather than fixed now, recorded so they aren't lost: (1) `pmmFreePages()`/`pmmAddFreeRange()` validate the Page-state machine *before* taking `pmmLock` -- correct today (nothing else can run concurrently to race it), but once IF turns on (M3.2) or SMP arrives (M3.4/M3.5), that read is no longer safe against a racing double free or an overlapping `pmmAddFreeRange()`. The fix at that point is to validate *under* the lock and call `pmmUnlock()` explicitly before any `pmmBug()` call (rather than relying on `archTrapCatch`'s longjmp to skip past it), not to move validation back outside the lock. (2) `pmmAddFreeRange()` only requires a range's frames to be `pmmPfnValid` and `PAGE_STATE_RESERVED` -- it can't distinguish genuinely-reclaimable memory (a LOADER_RECLAIM/ACPI_RECLAIM/INITRD region M2.3/M3.1/M5.5 means to hand over) from RESERVED-but-never-reclaimable memory that happens to share the same state (Page-array backing pages, page-table pages, the kernel image, or plain span-envelope padding over an MMIO/firmware hole). M2.2 itself never calls it with anything but freshly-scanned USABLE ranges, so this is safe today; M2.3/M3.1/M5.5 must pass only the exact ranges from `PmmMap`'s own recorded range lists (e.g. `loaderReclaim[]`), not a hand-rolled range, until a stronger check (e.g. a distinct `PAGE_F_RECLAIMABLE` flag set only on those ranges during `pmmMapScan`) exists | fixing either now, before any milestone actually exercises the risk (SMP, or a second `pmmAddFreeRange` caller) -- would be speculative hardening against a scenario M2.2 itself cannot trigger | Recording the exact failure mode and fix now, while the reasoning is fresh, costs a decision entry; rediscovering it during M2.3's or M3.4's own review pass would cost a repeat investigation. Found by the `reviewer` subagent (docs/logs/M2.2.md's reviewer-round entry) |
