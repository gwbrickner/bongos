# M2.4: Slab, kmalloc, vmalloc
**Branch:** `m2-4-slab-kmalloc-vmalloc` · **Started:** 2026-09-26 · **State:** in progress

## Summary
_Fill this in when the milestone is finished: what it delivers, in 3-6 lines. It becomes the release notes._

## Plan
ROADMAP M2.4 steps: (1) slab caches with constructors and per-CPU magazines; `kmalloc` size
classes 16-8192 bytes; (2) `vmalloc` for large, page-granular allocations with guard pages, built
on M2.3's `vmmMapKernel`/`vmmUnmapKernel` and KVA allocator (`kernel/mm/vmm.c`/`kva.c`); (3)
debug-build poisoning, redzones, and double-free/use-after-free detection; (4) ktests for stress,
alignment, redzone overflow, and a guard-page fault. Needs: M2.3 (done).

Sub-steps:
1. Consult the `architect` subagent for the design: slab cache/magazine layout, size-class table,
   `Page.privateWord` usage (reserved for this per page.h), the debug poison/redzone/double-free
   scheme and its relationship to D-082's `archTrapCatch`/`TRAP_CATCH_KERNEL_BUG`, and the
   kmalloc/vmalloc header API shape. Record as D-092+.
2. Implement the slab allocator core (cache create/alloc/free, per-CPU magazines -- BSP-only for
   now per M2.2's precedent, real per-CPU arrives M3.5).
3. Implement `kmalloc`/`kfree` on top of it (fixed size classes 16..8192).
4. Implement `vmalloc`/`vfree` on top of `vmmKvaAlloc`/`vmmMapKernel` (guard pages already built
   into `vmmKvaAlloc`).
5. Debug-build (`KERNEL_DEBUG`) poisoning/redzones/double-free/UAF detection, wired through
   `panicBug()`/`archTrapCatch` the way pmm's D-082 does.
6. ktests: stress, alignment guarantees, redzone-overflow detection, guard-page fault. Host tests
   for any pure logic (size-class math, free-list bookkeeping) that doesn't need real paging/pmm.
7. `qemu-tester`: `make test`/`make test-full`, `make host-tests`, `make format-check`.
8. `reviewer` pass(es); fix Critical/Should-fix.
9. Update ROADMAP.md/STATUS.md, write Summary, open PR (`needs-owner: yes`).

## Log
_Newest entry at the bottom. Add one entry per working step, and at least every ~30 minutes._

### 2026-09-26: milestone started
- **Changed:** created branch `m2-4-slab-kmalloc-vmalloc`, this log, plan above.
- **Why:** session protocol: M2.3 is done (PR #7 open), M2.4 is the next unchecked ROADMAP
  milestone whose Needs (M2.3) are satisfied.
- **Tests:** none yet.
- **Next step:** consult the `architect` subagent for the slab/kmalloc/vmalloc design.

### 2026-09-26 (2): architect design consulted, D-092..D-098 recorded
- **Changed:** consulted the `architect` subagent for the full slab/kmalloc/vmalloc design
  (magazine layout, size classes, bufctl scheme, redzone/poison scheme, `Page.privateWord`/
  `PageState` usage, header shape, and the KVA extent-table risk vmalloc exposes). Recorded
  D-092 through D-098 in DECISIONS.md, updated ARCHITECTURE.md §6.2 item 5/§3/§6.3 and
  `kernel/mm/README.md`'s no-recursion rule to match.
- **Why:** CLAUDE.md: consult `architect` before implementing a new kernel memory subsystem;
  this one also changes the pmm/vmm's own contracts (D-095/D-098), so needs-owner either way.
- **Tests:** none yet (docs only).
- **Next step:** implement D-095 (pmm page-ownership check) and D-098 (KVA liveCount cap)
  first -- small, self-contained changes to already-merged M2.2/M2.3 code -- then the new
  `kernel/mm/{slab,slab-core,slab-debug,kmalloc,vmalloc}.c`/`kernel/include/{kmalloc,vmalloc}.h`
  files per the architect's design (simplified in a few places for a tractable
  implementation -- see D-093/D-094/D-096's "simplified during implementation" notes).

### 2026-09-26 (3): D-095/D-098 implemented, host tests green
- **Changed:** `kernel/include/page.h` (`PAGE_F_SLAB`/`PAGE_F_VMALLOC`/`PAGE_F_OWNER_MASK`),
  `kernel/include/pmm.h` (`PMM_BUG_OWNED_PAGE`, appended), `kernel/mm/pmm.c`
  (`pmmValidateForFree()`'s new ALLOCATED-block ownership-bit check), `kernel/mm/kva-internal.h`/
  `kva.c` (`KvaState.liveCount`, the D-098 admission cap in `kvaAlloc()`, the decrement in
  `kvaFree()`), and `tests/host/kernel_kva_test.c` (replaced the now-unreachable
  `kvaFreeFailsWhenExtentTableFull` test with `kvaAllocRejectsAtLiveCap`/
  `kvaFreeNeverFailsUnderMaxFragmentation` -- explained in D-098 and in the test file itself).
  Also installed `libclang-rt-18-dev` in this container (STATUS.md's long-standing "Waiting on
  owner" gap -- `make host-tests` was failing to link ASan/UBSan without it; still needs the
  cloud setup-script re-run STATUS.md already tracks, this was just a per-container workaround).
- **Why:** ROADMAP M2.4 needs these two contract changes before the slab allocator/vmalloc can
  rely on them; per D-095/D-098's "needs-owner" notes.
- **Tests:** `make host-tests`: 163/163 pass (was 163 before too -- the replaced kva test is a
  1-for-1 swap, not a net addition, since the old test's premise no longer holds).
- **Next step:** implement the slab allocator core (`kernel/mm/slab-internal.h`,
  `slab-core.c`, `slab-debug.c`, `slab.c`), then `kmalloc.c`/`vmalloc.c`, then ktests.

## Decisions made
_Link the D-0xx entries added to DECISIONS.md._

## Owner hardware check
_Delete this section if the milestone has no hardware check._
