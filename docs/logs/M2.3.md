# M2.3: Kernel paging
**Branch:** `claude/vigilant-planck-fxpto9` (session-pinned; see note below) · **Started:** 2026-09-25 · **State:** in progress

_Note: this cloud session is pinned to branch `claude/vigilant-planck-fxpto9` rather than a fresh
`m2-3-kernel-paging` branch (its own environment instructions override CLAUDE.md's per-milestone
branch convention here). The branch already contains M1.1-M2.2 (merged to `main`) with nothing
else on top, so this is equivalent in practice._

## Summary
_Fill this in when the milestone is finished: what it delivers, in 3-6 lines. It becomes the release notes._

## Plan
_Break the roadmap steps into concrete sub-steps. Note which subagents you'll consult._
1. Consult `architect` for the paging design (CLAUDE.md consult-first: paging).
2. Build the kernel's own PML4 (HHDM, per-section kernel image perms, kernel-half PML4 slots).
3. Switch CR3, remove loader identity mapping, toggle CR4.PGE.
4. PAT setup; remap framebuffer WC.
5. SMEP/SMAP/UMIP.
6. `vmmMapKernel`/`vmmUnmapKernel` + kernel virtual area allocator.
7. ktests (W^X faults, WC PAT readback).
8. Reclaim LOADER_RECLAIM (step 6, D-083).
9. `qemu-tester` to run `make test`/`make test-full`.
10. `reviewer` pass, fix Criticals/Should-fix.

## Log
_Newest entry at the bottom. Add one entry per working step, and at least every ~30 minutes._

### <YYYY-MM-DD HH:MM>: <short title>
- **Changed:** files and areas
- **Why:**
- **Tests:** what ran, and the result
- **Next step:** precise enough for a fresh session to continue

## Decisions made
_Link the D-0xx entries added to DECISIONS.md._

## Owner hardware check
_Delete this section if the milestone has no hardware check._
- **Instructions:** step by step, for the owner
- **Result:** pending
