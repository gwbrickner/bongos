# M1.3: Kernel skeleton + real handoff
**Branch:** `claude/relaxed-curie-knpvwb` (cloud-session branch; see note below) · **Started:** 2026-09-24 · **State:** in progress

_Note: CLAUDE.md's session protocol names branches `m<p>-<n>-<slug>` (M1.2 used `m1-2-uefi-hello-loader`).
This cloud session was assigned a fixed branch, `claude/relaxed-curie-knpvwb`, by the harness, with an
explicit "never push to a different branch" instruction. Developing M1.3 there instead of creating
`m1-3-kernel-skeleton-real-handoff` to avoid violating that hard rule._

## Summary
_Fill in when done._

## Plan
Roadmap steps (ROADMAP.md M1.3):
1. Kernel side: `kernel.ld` (higher half at `0xFFFFFFFF80000000`, W^X section alignment), the
   entry stub (switch to kernel stack), the 16550 serial driver + `klog`, `panic()`, the ktest
   framework (`KTEST()` section registration, `ktest=` cmdline, isa-debug-exit reporting).
2. `boot/common/`: ELF64 loader, page-table builder (HHDM with 1 GiB/2 MiB pages, kernel mapping,
   trampoline identity page), BootInfo builder (`bootinfo.h` exactly as ARCHITECTURE §5.3),
   memory-map conversion.
3. UEFI loader: load `kernel.elf` + `boot.cfg` (`kernel=`/`cmdline=` only for now), call
   `ExitBootServices` with retry, jump to the kernel.
4. Kernel validates BootInfo (magic/version), prints the memory-map summary, runs its ktests.

**Done when:** `make test` shows `KTEST PASS bootinfo_valid` and exits 33 under OVMF at 1 CPU.

Touches the boot handoff ABI and the page-table builder (paging) -- both call for an `architect`
consult before implementation, per CLAUDE.md. Plan: consult `architect` for the concrete
handoff/paging/ktest design first, then implement, then `qemu-tester` to build/boot, then
`reviewer` before the PR.

## Log
_Newest entry at the bottom._

### 2026-09-24 (start): plan
- **Changed:** this log created, STATUS.md updated.
- **Why:** M1.2's PR (#2) merged to `main`; starting M1.3 per STATUS.md's Next step.
- **Tests:** none yet.
- **Next step:** consult `architect` for the BootInfo/page-table/ELF-loader/ktest design, then implement.

### 2026-09-24: architect design + first implementation pass
- **Changed:** consulted the `architect` subagent for the full M1.3 design (kernel.ld layout,
  entry-stub contract, BootInfo validation, ELF64 loader, page-table builder/HHDM coverage, ktest
  framework, memory-map conversion). Implemented from that design in a single pass:
  - `kernel/arch/x86_64/kernel.ld` + `entry.asm` (higher-half link, W^X PHDRs, boot stack with a
    guard gap, boot GDT + null IDT until M2.1), `kernel/arch/x86_64/qemu.c` (isa-debug-exit).
  - `kernel/core/`: `main.c` (kernelMain init order), `bootinfo.c` (validation contract),
    `klog.c`, `panic.c`, `format.c`, `cmdline.c`, `string.c`, `stack-protector.c`.
  - `kernel/drivers/serial/uart16550.c`, `kernel/test/ktest.c` + `.ktests`-section framework +
    `bootinfo_test.c`/`klog_test.c`.
  - `boot/common/`: `elf.c` (ELF64 loader, enforces W^X), `paging.c` (page-table builder, HHDM +
    kernel mapping + trampoline identity page), `memmap.c` (EFI-type mapping + rank-sweep
    normalize/coalesce), `bootcfg.c`, `bootmem.c`, `boot-status.c`, and `include/bootinfo.h`
    (§5.3 struct, byte-exact, with static asserts).
  - `boot/uefi/`: `trampoline.asm`, `file.c`/`handoff.c` (kernel.elf + boot.cfg loading, page
    tables, ExitBootServices retry loop, jump), `libc-shim.c` (memcpy/memset/etc. for the
    Windows-target build), and `main.c` wired to the new flow.
  - Build: `mk/kernel.mk` (new), edits to `mk/image.mk` (kernel build + trampoline + ktest image),
    `mk/test.mk` (drops the M1.2 `--expect-serial` bridge, uses the real isa-debug-exit/KTEST
    protocol), `mk/host-tests.mk` (new `boot/common` host tests).
  - `boot/boot.cfg`, `tests/harness/ktest-boot.cfg`.
  - `tests/host/boot_{elf,paging,memmap,bootcfg}_test.c` (38 new host tests).
  - `docs/DECISIONS.md`: D-059 through D-066 (HHDM coverage; EFI-type/rank-sweep memory-map
    normalization; kernel.ld layout; entry-stub boot GDT/null-IDT; ktest protocol/harness change;
    BootInfo v1 zero-means-absent + validation contract; boot/common portability rules;
    trampoline placement + handoff-block layout).
- **Why:** M1.3 per ROADMAP.md; touches paging and the boot handoff ABI, so the architect was
  consulted first per CLAUDE.md.
- **Tests:** `make host-tests`: 52/52 pass (14 pre-existing + 38 new). `make test`: `RESULT
  uefi-1cpu: PASS`, `MATRIX: PASS`; serial log shows `KTEST START/PASS bootinfo_valid`,
  `bootinfo_rejects_bad`, `memmap_coalesced`, `klog_format`, `KTEST DONE passed=4 failed=0` --
  the Done-when check. `make test-full` also passes (single-row matrix today). Independent
  verification via the `qemu-tester` subagent and a `reviewer` pass on the diff are in progress
  (not yet landed as of this commit) -- see the next log entry once they report back.
- **Deviations from the architect design:** fixed generous static bounds (1024-page PT pool,
  4096-entry BootInfo memmap array, 512-entry EFI-map arrays) instead of the exact formula sizes
  -- safe for QEMU's ~512 MiB default RAM, noted in `handoff.c`'s header as a simplification to
  revisit if a milestone needs more. `BOOT_ERR_NOT_MAPPED` added to `boot-status.h` (missing from
  the design's enum, needed by `ptLookup`). `kernel/include/arch/io.h` and
  `kernel/arch/x86_64/include/io-impl.h` use different filenames (a same-named wrapper/backend
  pair can't work with quoted includes) -- recorded under D-062. `memMapCheckOverlay()` and the
  final-EFI-map-is-a-subset-of-planned-runs re-check exist and are host-tested but aren't wired
  into the live loader path yet -- left for a follow-up to wire in.
- **Known gaps flagged for the owner (not blocking this milestone):**
  - A real hazard in ROADMAP.md's M2.2 step 4: it says to reclaim `LOADER_RECLAIM` "after
    switching stacks", but that memory holds the *live* page tables and the loader's identity
    page until the kernel builds and switches to its own CR3 in M2.3. Reclaiming it in M2.2 would
    free memory the CPU is still actively using for translation. Proposed fix: move the
    LOADER_RECLAIM reclaim to M2.3, after the kernel's own page tables are live and CR4.PGE has
    been toggled (needed anyway, since the loader's HHDM/kernel entries are marked Global).
  - `BootInfo.bootDiskGuid`/`bootPartGuid` (D-056) have no milestone that fills them yet; M1.3
    leaves them zero. Suggest M1.4 fills them via UEFI's PartitionInfo + a BlockIo read of the GPT
    header.
- **Next step:** get results back from the `qemu-tester` (independent build/boot verification)
  and `reviewer` (diff review) subagents, already running in the background; fix any Critical
  findings and re-review; then finish the milestone per CLAUDE.md (check ROADMAP.md's M1.3 box,
  write this log's Summary, update STATUS.md, open the PR with `needs-owner: yes`, and record the
  two owner-facing items above in STATUS.md's "Questions for owner"/"Waiting on owner").

## Decisions made
_Link the D-0xx entries added to DECISIONS.md._

## Owner hardware check
None for this milestone (ROADMAP.md's M1.3 entry has no owner hardware check listed; it's
marked `needs-owner` because it touches the boot handoff ABI and paging).
