# bongOS boot configuration (ARCHITECTURE §5.2). This is the minimal M1.3 subset (`kernel`/
# `cmdline` only); [entry] sections, resolution, kaslr, timeout, and default land in M1.4.
kernel = /bong/kernel.elf
cmdline =
